version: "2"
services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    command: [sh, -c, "yarn --silent && yarn run start & npm run forever"]
    ports:
      - "80:8888"
      - "443:8888"
    volumes:
      - ./:/app
